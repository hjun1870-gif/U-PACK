/**
 * config.example.js
 * Supabase 연동 설정 파일 템플릿
 * 
 * [사용 방법]
 * 1. 이 파일의 이름을 `config.js`로 변경합니다.
 * 2. Supabase 프로젝트 설정 -> API 메뉴에서 Project URL과 Anon Key를 복사하여 아래에 입력합니다.
 * 3. `config.js` 파일은 API 키 보안을 위해 .gitignore에 의해 GitHub 업로드 대상에서 제외됩니다.
 */

const SUPABASE_URL = "https://your-project-id.supabase.co";
const SUPABASE_ANON_KEY = "your-anon-key";
